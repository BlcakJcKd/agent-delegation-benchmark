# Task: debug a small Python package

Work only inside this workspace. Do not access parent directories or the network.

The `calcpack` package has deliberately seeded defects. Diagnose and fix the implementation, not the tests. Run `python -m unittest discover -s tests -q` and leave the test suite passing. Keep the public API unchanged and make only focused changes.
