# Task: concise Python data analysis

Work only inside this workspace. Do not access parent directories or the network.

`data/observations.csv` contains synthetic study measurements. Create `analysis.py` and `answer.json`. Use Python's standard library only. `answer.json` must contain numeric keys `n`, `mean_response`, `control_mean`, `treatment_mean`, and `difference`, where difference is treatment_mean minus control_mean. Round means/difference to two decimals. Briefly document your method in a module docstring. Do not modify the CSV.
