# Task: diagnostic matplotlib plot

Work only inside this workspace. Do not access parent directories or the network.

Use `data/qc_metrics.csv` to create `diagnostic.py`, `diagnostic.png`, and `summary.json`. The PNG must be a clearly labelled scatter plot of library size vs mapped reads, with sample labels and a visual indication of the most suspicious sample. In `summary.json`, give the keys `outlier_sample` and `outlier_reason`. Use the reason `low_library_size` when appropriate. Do not modify the input CSV.
