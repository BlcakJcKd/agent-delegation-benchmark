__pycache__/
.pytest_cache/
*.egg-info/
build/
runs/
delegate_runs/
/vllm_issues.jsonl

# Claude Code local session/runtime state (not project source).
.claude/

# Administrator-only ground truth and blind-review identity maps.
private_admin/
**/blind_map.json

# Delegation runtime configuration and private harness scratch copies belong in
# XDG user state, never in this source checkout.
/vllm.toml
/benchmark.toml
*.local.toml
*.private.toml
private_launchers/

# Local environment and credential material.  These extensions are not used by
# the benchmark's public fixtures or source tree.
.env
.env.*
credentials/
.credentials/
*.pem
*.p12
*.pfx
*.key
id_rsa
id_ed25519

# Python bytecode can be emitted by contestants and local tests.
*.py[cod]
